#pragma once
#include "Command.h"
class LedCommand : public Command {
public:
	//virtual void play() { actuator->Play(right_sample, left_sample); }
	//LedCommand(Discofier * actor, AudioSample right_sample, AudioSample left_sample);

private:
	//Discofier * actuator;
};

